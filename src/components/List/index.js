import React from 'react';
import { View,Text,StyleSheet,ScrollView } from 'react-native';

export default function List() {
 return (
   <ScrollView style={styles.list}>
     <Text>Sou um feed</Text>
   </ScrollView>
  );
};

const styles = StyleSheet.create({
  list:{
    

  }
});